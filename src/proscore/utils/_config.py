"""ProScore configuration constants."""

# Reference list of common special/missing-sentinel values.
# No longer used as a hard-coded default — users pass their own list to detect().
COMMON_SPECIAL_VALUES: list = [-999, -998, -1, 999999999]
